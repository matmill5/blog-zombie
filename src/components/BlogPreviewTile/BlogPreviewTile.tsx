import { FC } from "react";
import { BlogPreviewTileProps } from "./types";

const BlogPreviewTile: FC<BlogPreviewTileProps> = () => {
  return <>Blog Preview Tile</>;
};

BlogPreviewTile.defaultProps = {};

export default BlogPreviewTile;
